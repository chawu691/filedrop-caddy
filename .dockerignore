# Node modules
node_modules
backend/node_modules

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
logs
*.log
*-debug.log
*-error.log

# Build artifacts (Docker will build these)
dist_frontend
backend/dist

# Env files - IMPORTANT: Never commit .env files with secrets!
.env
.env.*
!.env.example
!.env.sample

# Docker files themselves (if in context root)
Dockerfile
Dockerfile.*
.dockerignore
.dockerignore.*
docker-compose.yml
docker-compose.yaml

# OS generated files
.DS_Store
Thumbs.db
._*

# IDE / Editor specific
.idea
.vscode
*.swp
*~
*.sublime-project
*.sublime-workspace

# Git / Version control
.git
.gitignore
.gitattributes
.hg
.svn

# Test reports / coverage
coverage
.nyc_output

# Temporary files / Caches
.eslintcache
.cache

# Sensitive data / Local data - these should be volumes in production
# Excluding contents from build context if they exist locally
backend/uploads/*
!backend/uploads/.gitkeep 
backend/database/*
!backend/database/.gitkeep
